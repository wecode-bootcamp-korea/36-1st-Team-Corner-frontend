import React from 'react';
import './ProductDetail.scss';

const ProductDetail = ({ detail }) => {
  return <div className="productDetail">ProductDetail{detail}</div>;
};

export default ProductDetail;
